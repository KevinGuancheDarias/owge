# Keep the docker build context small when building the backend image from this
# directory (the dev all-dockerized stack). target/ is ~GBs of build artifacts
# and the keys/env files are host-only secrets.
target/
.env.local
.env.dc14
