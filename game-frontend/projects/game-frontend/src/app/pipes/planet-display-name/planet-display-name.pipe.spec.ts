import { PlanetDisplayNamePipe } from './planet-display-name.pipe';

describe('PlanetDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PlanetDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
