import { DisplayUsernamePipe } from './display-username.pipe';

describe('DisplayUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
